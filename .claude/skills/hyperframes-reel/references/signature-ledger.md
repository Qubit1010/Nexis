# Signature Ledger

The one rule that makes every reel feel bespoke: **each reel gets its own signature motif, and no two
reels repeat it.** The genome (charcoal canvas, tech-blue voltage, QuicheSans/Urbanist, motion
doctrine) is constant; the *signature* — the central visual device and the world it lives in — is
invented fresh from each post and must be structurally distinct from everything already built.

**Before you design a new reel:** read this ledger. Pick a signature that is a different *kind of
object/world* from every row below — not a restyle of an existing one (a stack, an orb, a graph, a
panel, a chip, a heap, and a document are all different worlds; two "hub-and-spoke graphs" are the
same world).

**After you ship a reel:** append a row here. This file is the memory that keeps the batch diverse.

## Built so far

| Slug | Signature motif | World / device | Hard cuts |
|---|---|---|---|
| `ai-os` | orbital OS badge + flat 4-bar layer stack | an OS badge orbited by the Four C's | 1 |
| `claude-code-stack` | 3D isometric tool-slab stack on a glowing runtime core | isometric slabs stacking on a core | 0 |
| `claude-cowork` | messy heap → organized sandbox workspace demo (no orb) | a workspace tidying itself | 0 |
| `local-ai` | cloud/device perimeter geography + sealed on-device NPU chip die | a map with a severed perimeter + a chip | 1 (the "sever") |
| `claude-code-automation` | cursor-clicking UI that shatters vs a solid MCP hub-and-spoke connector graph | a breaking screen vs a plug-in hub | 0 |
| `ai-workspace` | one persistent panel: bolted-on plate → embedded concentric rings → invisible voltage seam | a single panel that transforms in place | 0 |
| `skillopt` | a readable `SKILL.md` document under an optimizer: coach → validation gate → accepted line locks in / rejected chip deflects to a reject buffer; opaque "MODEL WEIGHTS" black box as the foil | a text document + a diff + a gate (version-control / optimizer aesthetic) | 0 |
| `impeccable` | a generic "AI slop" UI mockup (icon tile + heading, purple→blue gradient band, card-in-card, gray text) scanned by a detector; named anti-pattern tell-flags flip `×`(amber) → `✓`(voltage) as command chips (`/audit /polish /bolder /quieter`) refine each element in place; the one mockup multiplies into a clone-wall as the "same templates" foil | a rendered UI mockup + a scanning detector reticle + named tell-flags (design-QA / linter aesthetic) | 0 |
| `nexis-os` | a lonely memory-less "chatbot" node that answers then **forgets** (drains to a gray husk, "memory: wiped") re-ignites and **grows** a full multi-cluster force-directed knowledge graph — MEMORY (never forgets) + SKILLS (50+ bloom) + SECOND BRAIN + PROJECTS hubs, edges drawing node-by-node into one pulsing living constellation; the recurring texture is one *disconnected* node vs a *dense connected* network | a growing multi-cluster force-directed knowledge-graph constellation (a living-network / AI-OS world; NOT a centered badge like ai-os, NOT a single hub-and-spoke like claude-code-automation) | 0 |
| `claude-obsidian` | notes rain into a dim grey **dead heap** ("find that note... → 0 results"), then one clean **source card** drops into a `.raw/` inbox and gets scanned, and **unfolds into ~12 readable Markdown page-cards** (each a `# heading` + body lines + a voltage **`[[wikilink]]`**) that **file themselves into `sources/ entities/ concepts/` folders** while thin voltage **link-threads draw page→page** into one living wiki; query it → an answer with `[[page]]` **citation chips**; punch = plain **.md you own** vs a struck-out "rented cloud app"; the recurring texture is the **`[[wikilink]]` double-bracket** (owned files becoming a graph) | a self-filing **library of readable rectangular Markdown pages + folders + `[[wikilinks]]`** (a filing / living-wiki world). **Explicitly NOT nexis-os's dot-and-edge force-graph constellation** — here the frame is always full of *documents you can read*, never abstract nodes; also NOT skillopt (one doc under a diff/accept gate) nor claude-cowork (generic desk-clutter tidying) | 0 |
| `claude-fable-5` | a **frontier-model control console** built around a big radial **EFFORT gauge** (LOW·MED·HIGH·XHIGH, tick marks, a glowing needle + a voltage fill-arc): the needle **sweeps LOW→XHIGH** as capability rises, an amber **cost meter** spikes beside it ("$50/M out", budget bar drains — the one warm accent), a **10-80-10 router** splits work-tokens to tier chips (`10% Fable 5·xhigh` / `80% Sonnet 5·Opus 4.8` / `10% verify`), then the dial face **irises open into a J-SPACE window** (word-thoughts "plan/verify/am I being tested?/goal" under an observe-reticle, honest caption **"access, not feelings"**); recurring texture = the **gauge/needle/tick-mark** (a cockpit-instrument aesthetic) | a **cockpit / instrument cluster** — a rotating radial dial + meters + a router + an observation window (a gauge/instrumentation world). **The one thing no prior reel has: a circular gauge with a sweeping needle + tick marks as the hero.** NOT `ai-workspace` (a single flat morphing panel), NOT `nexis-os`/`claude-obsidian` (nodes / document-cards), NOT `claude-code-automation` (a hub-and-spoke connector) | 0 |
| `outbound-personalized` | a **generic-message blast** sprays out and greys out ("IGNORED · ~3% reply"), then a **vertical pipeline track** draws in with 4 labeled **stations** (RESEARCH · SIGNAL · PERSONALIZE · ASK); a **pulse runs down the line** and a blank "New lead" card **fills station-by-station** — a dossier (Acme Robotics / VP Growth), a highlighted **signal chip** ("just posted 3 SDR roles"), a personalized opener, one consistent **ask** ("Worth a 15-min teardown?") — grey template → voltage **asset**, which **sends and gets a `↩ replied` ping**; then the line **runs many cards in parallel** ("3 signals → 2x replies", "scales without a rep"); recurring texture = a **lead-card advancing station-to-station, gaining a personalized layer at each stop** | a **production / assembly line** (a conveyor-pipeline world) — a vertical track + stations processing a lead-card that fills generic→personalized as it runs. **The one thing no prior reel has: a conveyor/pipeline with stations processing an item.** NOT `ai-workspace` (a single panel morphing *in place* — this card *runs a line through stations*), NOT `claude-obsidian`/`nexis-os` (filing/graph), NOT `skillopt` (one doc under a single gate), NOT `impeccable` (the generic-card foil here is a moving blast, not a static clone-grid) | 0 |
| `positioning-questions` | a **positioning statement that writes itself**: the hook is a **messy research pile** — scattered grey data fragments (bar chart, pie, quote card, "63%" stat, survey/checklist, sticky note) tumble in askew with a muted **"now what?"** tag; then the pile **shrinks and clears** and a **fill-in-the-blank statement template** draws in with **6 empty underlined blanks** ("For ___ who ___, we're the ___ that ___. Unlike ___, we ___."), a struck **"more research"** vs a lit **"six questions"** chip; at the peak the **six questions are asked one at a time** (a numbered caption flashes: "1 · Who is it really for?" ... "6 · Why you instead?") and each **blank fills with a crisp voltage answer** (busy founders / blend in / clear pick / gets chosen / more research / force the call), grey underline → filled word, until the **whole one-line statement glows**; punch = a grey **pile glyph** vs a voltage **checkmark** ("Research is a pile. Position is a decision."); outro = a small **statement mark** (two lines + a voltage underline). Recurring texture = **an empty underlined blank filling with a voltage answer as its question is asked** | a **fill-in-the-blank statement / form-completion** world — one sentence template whose 6 blanks write themselves from six answered questions. **The one thing no prior reel has: a Mad-Libs / fill-in-the-blank sentence assembling from Q&A.** NOT `claude-obsidian` (a library of readable page-cards filing — here it's ONE sentence with blanks, not many documents), NOT `skillopt` (a `SKILL.md` doc under a validation/accept gate — here a sentence completes from answers, no gate), NOT `no-code-content-team` (a hiring board of role cards — no cards here), NOT `outbound-personalized` (a conveyor where an item travels — nothing travels; blanks fill in place), NOT `nexis-os` (a node graph), NOT `claude-fable-5` (a gauge), NOT `impeccable` (a UI mockup under a detector) | 0 |
| `no-code-content-team` | a **staffing / hiring board**: five **HUMAN-HIRE cards** deal in — an avatar silhouette + role title (RESEARCHER · STRATEGIST · WRITER · DESIGNER · EDITOR) + a **salary pill** ($2k–$3k) — while a warm **PAYROLL / MONTH counter** shows **$12k**; a **CONTENT PLAN spreadsheet** docks at center with one **lit row** ("one row = one post"), a struck **"hire people"** vs a lit **"a pipeline"** chip; at the peak each card **flips in place human→AGENT** (a voltage node glyph + a role **verb** RESEARCH · BRIEF · WRITE · DESIGN · ASSEMBLE), **org-chart dispatch wires fan down from the sheet row to all five**, and every **salary strikes through**; then payroll **counts to $0**, the agents' output lands as one **PUBLISHED post card** + a **3-post scale strip** ("3x a week, while you work"), and the board dims to one lit **TASTE card** ("YOU KEEP / TASTE"); outro = a tiny **org-mark** (a sheet row + 3 agent dots). Recurring texture = **the role card flipping HUMAN + salary (grey) → AGENT + struck pay (voltage), reporting up a dispatch wire to the one spreadsheet row** | a **hiring board / team roster / org-chart** (a staffing world) — a spreadsheet control dispatching wires to a roster of role cards that flip from costly hires to automated agents. **The one thing no prior reel has: a staffing/hiring-board with a spreadsheet + salary tags + org-chart dispatch wires.** NOT `outbound-personalized` (a conveyor where ONE item travels a belt through stations — here FIVE persistent cards **flip in place** and report up to a central sheet, nothing travels), NOT `nexis-os` (an organic dot-and-edge constellation — here the nodes are deliberate labeled **role cards with avatars + salaries**, an org layout), NOT `claude-obsidian` (document page-cards filing), NOT `claude-fable-5` (a cockpit gauge), NOT `impeccable` (a UI mockup under a detector — though a card flips, the world is a **team of hires**, not a scanned UI), NOT `skillopt` (one doc under a single gate) | 0 |

## How to choose a fresh signature (method)

1. **Mine the post for its native object.** What is the post literally *about*? An OS → a badge/layers.
   A privacy shift → a perimeter/chip. A screen-clicking-vs-API argument → a breaking UI vs a connector
   graph. A text-file optimizer → a document + a gate. The best signature is the post's own subject
   rendered as a physical thing that moves.
2. **Name the recurring texture.** One small repeated element that carries the argument across beats —
   a port+plug (automation), a bolt vs an embedded trace (workspace), a diff line + a check (skillopt),
   an open padlock (local-ai). The texture is what makes the reel read as one idea, not six slides.
3. **Check it against this ledger.** If the object/world matches a row above, pick a different one.
4. **Decide the spine and the cut count.** 0 hard cuts is the default (continuous morph/reveal); use
   the single allowed hard cut only when the story has a real "sever/break" beat (local-ai, ai-os).
5. **Write it into `STORYBOARD.md`** with an explicit "this is deliberately different from [prior
   reels]" paragraph, so the distinctness is a stated design constraint, not an accident.

The genome never changes; the signature never repeats. That combination is the whole look.
