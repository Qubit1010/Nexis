# Signature Ledger

The one rule that makes every reel feel bespoke: **each reel gets its own signature motif, and no two
reels repeat it.** The genome (charcoal canvas, tech-blue voltage, QuicheSans/Urbanist, motion
doctrine) is constant; the *signature* — the central visual device and the world it lives in — is
invented fresh from each post and must be structurally distinct from everything already built.

**Before you design a new reel:** read this ledger. Pick a signature that is a different *kind of
object/world* from every row below — not a restyle of an existing one (a stack, an orb, a graph, a
panel, a chip, a heap, and a document are all different worlds; two "hub-and-spoke graphs" are the
same world).

**After you ship a reel:** append a row here. This file is the memory that keeps the batch diverse.

## Built so far

| Slug | Signature motif | World / device | Hard cuts |
|---|---|---|---|
| `ai-os` | orbital OS badge + flat 4-bar layer stack | an OS badge orbited by the Four C's | 1 |
| `claude-code-stack` | 3D isometric tool-slab stack on a glowing runtime core | isometric slabs stacking on a core | 0 |
| `claude-cowork` | messy heap → organized sandbox workspace demo (no orb) | a workspace tidying itself | 0 |
| `local-ai` | cloud/device perimeter geography + sealed on-device NPU chip die | a map with a severed perimeter + a chip | 1 (the "sever") |
| `claude-code-automation` | cursor-clicking UI that shatters vs a solid MCP hub-and-spoke connector graph | a breaking screen vs a plug-in hub | 0 |
| `ai-workspace` | one persistent panel: bolted-on plate → embedded concentric rings → invisible voltage seam | a single panel that transforms in place | 0 |
| `skillopt` | a readable `SKILL.md` document under an optimizer: coach → validation gate → accepted line locks in / rejected chip deflects to a reject buffer; opaque "MODEL WEIGHTS" black box as the foil | a text document + a diff + a gate (version-control / optimizer aesthetic) | 0 |
| `impeccable` | a generic "AI slop" UI mockup (icon tile + heading, purple→blue gradient band, card-in-card, gray text) scanned by a detector; named anti-pattern tell-flags flip `×`(amber) → `✓`(voltage) as command chips (`/audit /polish /bolder /quieter`) refine each element in place; the one mockup multiplies into a clone-wall as the "same templates" foil | a rendered UI mockup + a scanning detector reticle + named tell-flags (design-QA / linter aesthetic) | 0 |
| `nexis-os` | a lonely memory-less "chatbot" node that answers then **forgets** (drains to a gray husk, "memory: wiped") re-ignites and **grows** a full multi-cluster force-directed knowledge graph — MEMORY (never forgets) + SKILLS (50+ bloom) + SECOND BRAIN + PROJECTS hubs, edges drawing node-by-node into one pulsing living constellation; the recurring texture is one *disconnected* node vs a *dense connected* network | a growing multi-cluster force-directed knowledge-graph constellation (a living-network / AI-OS world; NOT a centered badge like ai-os, NOT a single hub-and-spoke like claude-code-automation) | 0 |
| `claude-obsidian` | notes rain into a dim grey **dead heap** ("find that note... → 0 results"), then one clean **source card** drops into a `.raw/` inbox and gets scanned, and **unfolds into ~12 readable Markdown page-cards** (each a `# heading` + body lines + a voltage **`[[wikilink]]`**) that **file themselves into `sources/ entities/ concepts/` folders** while thin voltage **link-threads draw page→page** into one living wiki; query it → an answer with `[[page]]` **citation chips**; punch = plain **.md you own** vs a struck-out "rented cloud app"; the recurring texture is the **`[[wikilink]]` double-bracket** (owned files becoming a graph) | a self-filing **library of readable rectangular Markdown pages + folders + `[[wikilinks]]`** (a filing / living-wiki world). **Explicitly NOT nexis-os's dot-and-edge force-graph constellation** — here the frame is always full of *documents you can read*, never abstract nodes; also NOT skillopt (one doc under a diff/accept gate) nor claude-cowork (generic desk-clutter tidying) | 0 |
| `claude-fable-5` | a **frontier-model control console** built around a big radial **EFFORT gauge** (LOW·MED·HIGH·XHIGH, tick marks, a glowing needle + a voltage fill-arc): the needle **sweeps LOW→XHIGH** as capability rises, an amber **cost meter** spikes beside it ("$50/M out", budget bar drains — the one warm accent), a **10-80-10 router** splits work-tokens to tier chips (`10% Fable 5·xhigh` / `80% Sonnet 5·Opus 4.8` / `10% verify`), then the dial face **irises open into a J-SPACE window** (word-thoughts "plan/verify/am I being tested?/goal" under an observe-reticle, honest caption **"access, not feelings"**); recurring texture = the **gauge/needle/tick-mark** (a cockpit-instrument aesthetic) | a **cockpit / instrument cluster** — a rotating radial dial + meters + a router + an observation window (a gauge/instrumentation world). **The one thing no prior reel has: a circular gauge with a sweeping needle + tick marks as the hero.** NOT `ai-workspace` (a single flat morphing panel), NOT `nexis-os`/`claude-obsidian` (nodes / document-cards), NOT `claude-code-automation` (a hub-and-spoke connector) | 0 |
| `outbound-personalized` | a **generic-message blast** sprays out and greys out ("IGNORED · ~3% reply"), then a **vertical pipeline track** draws in with 4 labeled **stations** (RESEARCH · SIGNAL · PERSONALIZE · ASK); a **pulse runs down the line** and a blank "New lead" card **fills station-by-station** — a dossier (Acme Robotics / VP Growth), a highlighted **signal chip** ("just posted 3 SDR roles"), a personalized opener, one consistent **ask** ("Worth a 15-min teardown?") — grey template → voltage **asset**, which **sends and gets a `↩ replied` ping**; then the line **runs many cards in parallel** ("3 signals → 2x replies", "scales without a rep"); recurring texture = a **lead-card advancing station-to-station, gaining a personalized layer at each stop** | a **production / assembly line** (a conveyor-pipeline world) — a vertical track + stations processing a lead-card that fills generic→personalized as it runs. **The one thing no prior reel has: a conveyor/pipeline with stations processing an item.** NOT `ai-workspace` (a single panel morphing *in place* — this card *runs a line through stations*), NOT `claude-obsidian`/`nexis-os` (filing/graph), NOT `skillopt` (one doc under a single gate), NOT `impeccable` (the generic-card foil here is a moving blast, not a static clone-grid) | 0 |
| `positioning-questions` | a **positioning statement that writes itself**: the hook is a **messy research pile** — scattered grey data fragments (bar chart, pie, quote card, "63%" stat, survey/checklist, sticky note) tumble in askew with a muted **"now what?"** tag; then the pile **shrinks and clears** and a **fill-in-the-blank statement template** draws in with **6 empty underlined blanks** ("For ___ who ___, we're the ___ that ___. Unlike ___, we ___."), a struck **"more research"** vs a lit **"six questions"** chip; at the peak the **six questions are asked one at a time** (a numbered caption flashes: "1 · Who is it really for?" ... "6 · Why you instead?") and each **blank fills with a crisp voltage answer** (busy founders / blend in / clear pick / gets chosen / more research / force the call), grey underline → filled word, until the **whole one-line statement glows**; punch = a grey **pile glyph** vs a voltage **checkmark** ("Research is a pile. Position is a decision."); outro = a small **statement mark** (two lines + a voltage underline). Recurring texture = **an empty underlined blank filling with a voltage answer as its question is asked** | a **fill-in-the-blank statement / form-completion** world — one sentence template whose 6 blanks write themselves from six answered questions. **The one thing no prior reel has: a Mad-Libs / fill-in-the-blank sentence assembling from Q&A.** NOT `claude-obsidian` (a library of readable page-cards filing — here it's ONE sentence with blanks, not many documents), NOT `skillopt` (a `SKILL.md` doc under a validation/accept gate — here a sentence completes from answers, no gate), NOT `no-code-content-team` (a hiring board of role cards — no cards here), NOT `outbound-personalized` (a conveyor where an item travels — nothing travels; blanks fill in place), NOT `nexis-os` (a node graph), NOT `claude-fable-5` (a gauge), NOT `impeccable` (a UI mockup under a detector) | 0 |
| `video-as-code` | a **twin-panel seek rig**: a browser PREVIEW pane above and an OUTPUT MP4 pane below, bound by a tether and a frame-index ruler; an NLE timeline deletes itself to make room, a struck-through "▶ PLAY" chip sets up the claim, then a **discrete stepper** jumps tick-to-tick (literal `steps()` easing, never a smooth sweep) and each jump **stamps an identical frame** into the output with an "=" pixel-match tick, counter running to 300 | two bound panels + discrete indexed stepping (a capture-rig world). NOT a conveyor (nothing travels station to station), NOT a loop (strictly linear along a ruler), NOT a filling bar (a ruler being *indexed along*, two panels not one). **v1 of this slug was a code-bracket filmstrip — rejected for weak script + visuals and fully replaced from the Doc's Source_S tab; do not revive it.** | 0 |
| `marketing-report` | a **tripwire stretched across a metric line**: 13 grey tangled polylines snarl over each other (the forty-chart report nobody reads), then all but one dissolve and a single **main line** is left running left-to-right; a **dashed voltage tripwire** draws in at a threshold height, an amber **cost meter** ticks beside it, and when the line finally crosses the wire a **crossdot fires** — the rule catching the thing. The foil is a greyed **ghost block** with a month strip (JAN·FEB·MAR·APR) where an amber **late-flag** only lights on APR: same break, found a quarter later. Rules stack as labeled rows; the proof lands as a single figure (90 hrs/week). Recurring texture = **a line crossing a dashed threshold and tripping a dot** | an **alarm / threshold-tripwire** world (a monitored line that trips a wire the moment it crosses). **v1 of this slug was a 30-tile data-wall under a cascading cull — replaced along with its script when the reel was rebuilt from Source_S; do not revive it.** NOT `kpi-stack` (two bubbles arguing toward one number — no monitored line, no threshold), NOT `claude-opus-5` (a single capacity bar filling until text clips — here a line *travels* and crosses a wire), NOT `outbound-personalized` (a conveyor with stations) | 0 |
| `claude-opus-5` | a **vertical paper tape / token ledger** that unspools down the frame and prints entries onto itself: first the numbers everyone posted (43.3% coding, 2x prior, $5/$25 unchanged), which scroll away and are replaced by the two that reach the invoice (−60% time, −1/3 tool calls) under a voltage `PER TASK` bracket; then an amber dashed **`MAX_TOKENS` cap line** lands across the tape and **THINKING** grey bars and an **ANSWER** paragraph print onto the *same* tape, until the answer's last line dies mid-word (`in an inconsisten`) exactly at the cap, the tape **tears off** on a ragged edge, and a green **✓ SUCCESS** stamp thumps down askew straddling the tear. The outro **slides the cap down** (amber → voltage), the tape extends past the tear, and the severed sentence completes. Recurring texture = **a printed tape line meeting a hard cap edge** | a **paper-tape / receipt-ledger** world — one continuous consumable strip printed on by two competing consumers and physically severed at a limit. **v1 of this slug was a thin horizontal capacity bar; rejected for weak script AND visuals, fully rebuilt from the Doc's Source_S tab (v1 archived at `archives/hyperframes-reel-claude-opus-5-v1-2026-07-29/`); do not revive it.** NOT `claude-fable-5` (a radial cockpit gauge), NOT `claude-obsidian` (many discrete document-cards filing into folders — this is ONE continuous strip), NOT `skillopt` (a doc under an accept/reject gate — nothing is judged here, it just runs out), NOT `marketing-report` (a line crossing a tripwire), NOT `outbound-personalized` (a conveyor carrying an item through stations) | 0 |
| `kpi-stack` | two grey "FEELING" speech-bubbles jostle and clash unresolved over a scatter of 30+ vague number-chips, until one glowing verified ratio-chip (3.2:1) drops between them and both bubbles snap into aligned voltage agreement with a checkmark | a debate / argument-resolution world (two opinions converging on one number) | 0 |
| `agents-not-chatbots` | a closed-loop retry racetrack: a task-card laps a rectangular track through one check-gate repeatedly, ticking a running-cost meter each pass, contrasted with a single chatbot speech-bubble that never loops; a **one-year trajectory bar** (12% → 66% desktop-task success, fill and counter animating together) lands in the punch beat *before* the resolution, then two parallel mini-loops resolve it — a "brilliant" card gets pulled aside by a hand after a miss, a "dull" card exits stamped DONE | a closed-loop / repeated-retry world (same gate revisited, cost accumulating; explicitly not a straight conveyor). The trajectory bar is a supporting instrument inside this world, not a second signature — it never becomes the hero the way `claude-opus-5`'s capacity bar does | 0 |
| `no-code-content-team` | a **staffing / hiring board**: five **HUMAN-HIRE cards** deal in — an avatar silhouette + role title (RESEARCHER · STRATEGIST · WRITER · DESIGNER · EDITOR) + a **salary pill** ($2k–$3k) — while a warm **PAYROLL / MONTH counter** shows **$12k**; a **CONTENT PLAN spreadsheet** docks at center with one **lit row** ("one row = one post"), a struck **"hire people"** vs a lit **"a pipeline"** chip; at the peak each card **flips in place human→AGENT** (a voltage node glyph + a role **verb** RESEARCH · BRIEF · WRITE · DESIGN · ASSEMBLE), **org-chart dispatch wires fan down from the sheet row to all five**, and every **salary strikes through**; then payroll **counts to $0**, the agents' output lands as one **PUBLISHED post card** + a **3-post scale strip** ("3x a week, while you work"), and the board dims to one lit **TASTE card** ("YOU KEEP / TASTE"); outro = a tiny **org-mark** (a sheet row + 3 agent dots). Recurring texture = **the role card flipping HUMAN + salary (grey) → AGENT + struck pay (voltage), reporting up a dispatch wire to the one spreadsheet row** | a **hiring board / team roster / org-chart** (a staffing world) — a spreadsheet control dispatching wires to a roster of role cards that flip from costly hires to automated agents. **The one thing no prior reel has: a staffing/hiring-board with a spreadsheet + salary tags + org-chart dispatch wires.** NOT `outbound-personalized` (a conveyor where ONE item travels a belt through stations — here FIVE persistent cards **flip in place** and report up to a central sheet, nothing travels), NOT `nexis-os` (an organic dot-and-edge constellation — here the nodes are deliberate labeled **role cards with avatars + salaries**, an org layout), NOT `claude-obsidian` (document page-cards filing), NOT `claude-fable-5` (a cockpit gauge), NOT `impeccable` (a UI mockup under a detector — though a card flips, the world is a **team of hires**, not a scanned UI), NOT `skillopt` (one doc under a single gate) | 0 |
| `website-creator` | a **drill core extracted from a live site**: a scrape line cuts across a rendered body, a bit descends at a constant rate against a **depth ruler**, and the extracted column **splits into six labeled strata** (palette, type, spacing, layout, copy, gradients), each band pairing with its measured value — grey and unlabeled above the scrape line, voltage and measured below it. Recurring texture = **a stratum band pairing with its measured value against a depth ruler** | a **geology / core-sampling** world — material physically removed from a source body and read by depth. **The one thing no prior reel has: geology, a depth ruler, an extracted stratified core.** NOT `impeccable` (a UI mockup *scanned* by a detector, flags flipping in place — nothing is scanned here, material is *removed*), NOT `positioning-questions` (blanks completing in a sentence template — the motion here is extraction from depth, not completion), NOT `claude-opus-5` (one continuous tape read by length — this column is discrete labeled strata read by depth), NOT `claude-code-stack` (isometric slabs *stacking onto* a core — here layers are already buried and get *cut out and pulled up*, opposite direction), NOT `outbound-personalized` (a conveyor — nothing travels, one body is sampled in place) | 0 |
| `integration-powerups` | a **passport gaining border stamps**: a bound booklet accumulates discrete recognitions — grey hand-written visas before the standard, voltage machine-struck stamps after ratification — each stamp thumping in while a gate arm lifts behind it. The "smarter model" foil is a stamped-on version badge that opens no border. Recurring texture = **a stamp thumping into the document while a gate lifts behind it** | a **border-control / travel-document** world — authority arriving from *external recognition*, never from a connection. **Deliberately has no hub, no spokes, no plug, no socket**, because `claude-code-automation` already owns the MCP hub-and-spoke graph and the ledger's own method section claims port+plug as the automation texture. NOT `claude-fable-5` (a cockpit gauge with a sweeping needle — the model foil here is a badge, never a dial turned up), NOT `outbound-personalized` (a conveyor through stations — the passport never travels, borders open around it), NOT `no-code-content-team` (a roster of cards — one object here, not a team), NOT `claude-opus-5` (one continuous tape, severed — this is a bound booklet of discrete pages, never cut) | 0 |
| `skills-not-docs` | **a line of innocuous text lifting to reveal a reaching arm underneath**: a readable markdown card sits benign, then its visible layer **peels up like a lid** and voltage tendrils extend out from under it to grip three named targets (a shell, a filesystem path, a network call) — grey while it looks like a document, voltage the moment it acts. Recurring texture = **a text line peeling up + tendrils reaching out to grip a target** | a **two-layer object whose visible layer is a lie** (a disguise / concealment world). **The one thing no prior reel has: a lid, and a thing underneath it.** NOT `skillopt` (a `SKILL.md` *judged* by a validation gate — there the doc is the optimizer's subject and nothing is hidden; here the doc is a **lid**), NOT `impeccable` (a UI mockup under a scanning reticle, flags flipping — nothing is scanned, the card opens itself), NOT `claude-obsidian` (many page-cards filing into folders — those stay benign documents; this one stops being a document at beat 3), NOT `hermes-agent` (a workshop forging unstamped parts — that reel is about **provenance** of new parts, this one about **disguise** of existing ones) | 0 |
| `hermes-agent` | a **machine tool that forges its own cutting bits**: a plated shop machine stalls amber on a HARD PROBLEM slab, **pours and cools a new bit**, seats it in the chuck, cuts the slab clean, and drops the bit into a rack slot tagged `skill saved` — then the rack is inspected and the factory-supplied bits carry a **REVIEWED** stamp while the self-forged one sits in the chuck **unstamped**, ringed amber. Voltage means "made by itself", so the only voltage-bright object in the reel is exactly the thing being questioned. Recurring texture = **a bit seating into the chuck, and whether it carries an inspection stamp** | a **machine shop / manufacturing** world — a tool that permanently changes itself by making new tooling; the workpiece never moves, the tooling above it does. **The one thing no prior reel has: a workshop, a chuck, or a manufactured part.** NOT `agents-not-chatbots` (a task-card lapping a closed track through the same gate — nothing is manufactured, the same item goes round; here each pass **leaves a new physical part behind**), NOT `skills-not-docs` (a document whose visible layer is a disguise — this reel hides nothing, you watch the part get made: **provenance**, not concealment), NOT `skillopt` (one doc under an accept/reject gate — a gate judges, a forge produces), NOT `outbound-personalized` (a conveyor carrying an item), NOT `claude-opus-5` (a consumable tape severed — nothing is consumed, stock accrues). **A June Remotion `content.json` existed for this slug and was rejected, not reused** — it carried a claim absent from the Aug 14 source; archived at `archives/hermes-agent-remotion-2026-06-22/`. | 0 |
| `smart-goals` | a **fogged block cleaving into twelve checkable week-slots**: a sealed hatched block reading `GET MORE CUSTOMERS` cannot be seen into or ticked (a `check it this week?` tag lands and **slides off** for want of a handle), then the fog clears into one legible figure (`40 qualified leads · by end of Q1`), a cleave line runs through it and it **splits into a 12-slot tray** (`W1`–`W12`, each holding `3-4`) that ticks slot-by-slot; the punch squares a greyed **wish** (one check, twelve weeks out) against a lit **system** (twelve checks, one a week). Recurring texture = **a slot receiving a check** — grey and sealed while the goal is a wish, voltage the moment a slot can actually be ticked | a **division / subdivision** world — one solid becoming twelve separately touchable things; the argument is not that the goal got better but that it got **divisible**, and the object is a unit of **time**. **The one thing no prior reel has: a divided tray, or time as the object.** NOT `marketing-report` (a monitored line crossing a dashed tripwire — no line here, nothing watched for a breach; a solid is **cut into parts**), NOT `kpi-stack` (two bubbles *converging* on one verified number — this **starts** with the figure and divides it), NOT `positioning-questions` (six blanks filling inside a sentence template — blanks complete a sentence; here a block is **subdivided into a tray**, fission not fill), NOT `website-creator` (a stratified core **extracted from** a body — that is removal from depth, this is division of a whole; the core keeps its layers, this block becomes twelve separate things), NOT `no-code-content-team` (a roster of role cards — these slots are **time**, not people, and are ticked rather than flipped) | 0 |

## How to choose a fresh signature (method)

1. **Mine the post for its native object.** What is the post literally *about*? An OS → a badge/layers.
   A privacy shift → a perimeter/chip. A screen-clicking-vs-API argument → a breaking UI vs a connector
   graph. A text-file optimizer → a document + a gate. The best signature is the post's own subject
   rendered as a physical thing that moves.
2. **Name the recurring texture.** One small repeated element that carries the argument across beats —
   a port+plug (automation), a bolt vs an embedded trace (workspace), a diff line + a check (skillopt),
   an open padlock (local-ai). The texture is what makes the reel read as one idea, not six slides.
3. **Check it against this ledger.** If the object/world matches a row above, pick a different one.
4. **Decide the spine and the cut count.** 0 hard cuts is the default (continuous morph/reveal); use
   the single allowed hard cut only when the story has a real "sever/break" beat (local-ai, ai-os).
5. **Write it into `STORYBOARD.md`** with an explicit "this is deliberately different from [prior
   reels]" paragraph, so the distinctness is a stated design constraint, not an accident.

The genome never changes; the signature never repeats. That combination is the whole look.
