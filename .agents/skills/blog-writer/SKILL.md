---
name: blog-writer
description: "Research-backed long-form blog engine for Aleem's personal brand and for client brands. Writes a complete SEO and AI-search optimized post that reads unmistakably human, in the resolved brand voice (Aleem's by default, a client's when a slug is named). Engineered for citation by answer engines: answer-first structure, query fan-out coverage, extractable blocks, comparison tables, FAQ and schema. Validates its own output through seo-onpage before finishing. Say 'write a blog post on X', 'draft an article about X'. For shorter formats use content-production; for the plan behind it, content-strategy."
argument-hint: [topic, or "from scratch" + niche/angle, or "for <client-slug>"]
---

# Pointer to `.claude/skills/blog-writer/`

This file is a pointer, not a copy. Read and follow the canonical skill at
`.claude/skills/blog-writer/SKILL.md` (path from the repository root), and resolve every file it
references relative to that canonical folder, not this one.

Generated by `scripts/sync_agents_mirror.py`. Edit the canonical skill, never this file.
