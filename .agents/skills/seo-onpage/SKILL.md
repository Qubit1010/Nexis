---
name: seo-onpage
description: "Use to OPTIMIZE or AUDIT the on-page and content layer: a single page, a whole site, or an unpublished blog draft. Parses real HTML locally, runs Lighthouse, computes actual image savings, and extracts the term gap against the live top 10. Four verdicts where 'review' and 'unknown' are first-class, so a judgment call is never reported as a pass. Outputs a 6-tab Sheet plus a report. Tier 2 of the SEO course. For SEO theory use seo-advisor; keywords, seo-foundation; crawlability and schema, seo-technical; to write the article, blog-writer."
argument-hint: [URL, client-projects slug, or a draft path - optionally "audit" / "optimize" / "refresh"]
---

# Pointer to `.claude/skills/seo-onpage/`

This file is a pointer, not a copy. Read and follow the canonical skill at
`.claude/skills/seo-onpage/SKILL.md` (path from the repository root), and resolve every file it
references relative to that canonical folder, not this one.

Generated by `scripts/sync_agents_mirror.py`. Edit the canonical skill, never this file.
