# === PRIVATE — Drive-only, never committed (keep in sync with .claude/rules/privacy.md) ===
# Edit these to match this OS's sensitive paths:
# context/<sensitive>.md
# <private-folder>/
decisions/log.md

# Secrets & local overrides
.env
.env.*
!.env.example
CLAUDE.local.md

# Tooling
__pycache__/
*.pyc
.venv/
venv/
node_modules/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
